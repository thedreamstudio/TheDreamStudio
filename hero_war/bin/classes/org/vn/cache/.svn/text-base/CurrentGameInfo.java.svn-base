package org.vn.cache;

import java.util.ArrayList;

import org.vn.model.Enemy;
import org.vn.model.EnemyType;
import org.vn.model.Map;
import org.vn.model.PlayerModel;

/** Save all information of game */
public class CurrentGameInfo {
	private static CurrentGameInfo intance = new CurrentGameInfo();

	private CurrentGameInfo() {
	}

	public static CurrentGameInfo getIntance() {
		return intance;
	}

	public int boardId;
	/** ID của chủ bàn */
	public int ownerId;
	public int gold;
	public ArrayList<PlayerModel> mListPlayerInGame = new ArrayList<PlayerModel>();
	public ArrayList<EnemyType> listEnemytype = new ArrayList<EnemyType>();
	public ArrayList<Enemy> listIdEnemyChosse = new ArrayList<Enemy>();
	public ArrayList<Map> listMaps = new ArrayList<Map>();
	public ArrayList<Enemy> listIdEnemyInMap = new ArrayList<Enemy>();

	public Map mMapSelected = null;
	public int xTileKing;
	public int yTileKing;
	public int rangerSetupEnemy;

	public void reset() {
		mListPlayerInGame.clear();
		mMapSelected = null;
		listIdEnemyInMap.clear();
		listIdEnemyChosse.clear();
	}

	/*
	 * Tra ve so tien con lai sau khi mua enemy
	 */
	// public int getGoldRemain() {
	// int goldTamp = gold;
	// for (EnemyType enemy : listEnemytype) {
	// goldTamp -= enemy.cost * enemy.countChoose;
	// }
	// return goldTamp;
	// }

	// public Point getPosTileRandom() {
	// int x = 0;
	// int y = 0;
	// int minX = Math.min(xTileKing, 0);
	// int maxX = Math.min(xTileKing, mMapSelected.mapType.row);
	// do {
	// while (true) {
	// if (Utils.RANDOM.nextInt(2) == 0) {
	// x = xTileKing + 1 + Utils.RANDOM.nextInt(maxX - xTileKing);
	// } else {
	//
	// }
	// break;
	// }
	// while (true) {
	// y = Utils.RANDOM.nextInt(rangerSetupEnemy)
	// * (Utils.RANDOM.nextInt(2) == 0 ? 1 : -1) + yTileKing;
	// if (y < 0 || y >= mMapSelected.mapType.column) {
	// continue;
	// }
	// break;
	// }
	// // kiem tra co linh tai vi tri do chua
	// for (Enemy enemy : listIdEnemyChosse) {
	// if (enemy.xTile == x && enemy.yTile == y) {
	// continue;
	// }
	// }
	// return new Point(x, y);
	// } while (true);
	// }
}
